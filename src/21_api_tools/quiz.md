# API Tools — Connecting LLMs to External Services — Quiz

## Multiple Choice (answer before checking)

**Q1**: What does the `|` operator do in LCEL?
- A) Concatenates strings
- B) Creates a RunnableSequence
- C) Calls the LLM
- D) Parses output

**Q2**: What type does ChatOpenAI.invoke() return?
- A) str
- B) dict
- C) AIMessage
- D) ChatResponse

**Q3**: Why use PromptTemplate instead of f-strings?
- A) It's faster
- B) It's composable and validates variables
- C) It's required by the API
- D) It translates text

**Q4**: What is a LangGraph State?
- A) The current mode of the LLM
- B) Shared TypedDict passed between all nodes
- C) The checkpoint data
- D) The agent's memory

## Answers (don't peek!)
||
||
|| Q1: B | Q2: C | Q3: B | Q4: B
